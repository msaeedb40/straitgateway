kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
networking:
  kubeProxyMode: "none" # required for CNI straitgateway
  disableDefaultCNI: true # required for CNI straitgateway
nodes:
  - role: control-plane
  - role: worker
  - role: worker
